---
switches:
  - hostname: string
    device_function: string
    mgmt_ip: ip address
    bgp_asn: int
    bgp_id: ip address
    vtep_ip: ip address
hostname:
  switches:
    type: dict
    hostname:
      type: string

