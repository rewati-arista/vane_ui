- name: test_host.py
  testcases:
    - name: test_if_hostname_is_correcet_on_
      description: Verify hostname is set correctly on device
      # DUT name is taken from the definitions file
      expected_output: null
      show_cmd: show hostname
      report_style: modern
      test_criteria: Verify hostname is set correctly on device
      # Optionally filter duts by criteria: name, role, regex, or names
      criteria: names
      filter: 
        - DLFW3
        - DLFW4
