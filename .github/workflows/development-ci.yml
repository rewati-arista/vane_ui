name: development-ci
run-name: ${{ github.actor }} CI development run

on: [push]

jobs:
  validate:

    runs-on: ubuntu-latest

    steps:
      # Check out the repository
      - uses: actions/checkout@v3

      - name: Install Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          # architecture: 'x64' # The default

      - name: Display the Python version
        run: python -c "import sys; print(sys.version)"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install black==22.12.0 flake8==5.0.4 pylint==2.16.2

      # Run Black
      - name: Run Black to check the code for formatting problems
        # If any code needs reformatting, black returns an exit code of 1, which
        # fails the action
        run: black --check .
        continue-on-error: true

      # Run flake8
      - name: Lint with flake8
        run: flake8 ./vane ./tests
        continue-on-error: true

      # Run pylint
      - name: Lint with pylint
        run: pylint ./vane ./tests
        continue-on-error: true
